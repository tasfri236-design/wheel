fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
pydantic>=2,<3

# Raspberry Pi hardware (install on device; skip on dev Mac if using MOCK_TELEMETRY=1)
Adafruit-Blinka>=8
adafruit-circuitpython-bno08x>=1.3.0
gpiozero>=2.0

# Camera ball-follow (follow_ball.py). On a Pi you can instead run
# `sudo apt install -y python3-opencv` and drop opencv-python-headless here.
opencv-python-headless>=4.9
imutils>=0.5
requests>=2.31
